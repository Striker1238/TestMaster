<Page x:Class="TestMaster.Views.TestCreatorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:TestMaster.Views"
      mc:Ignorable="d" 
      Title="TestMaster" Height="600" Width="900"
      Background="{DynamicResource BaseWhite}">
    <Grid>
        <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" Background="{DynamicResource PrimaryDark}" Height="40" DockPanel.Dock="Top">
                <Button Content="Вернуться к тестам" Click ="BackButton_Click" Margin="0,0,10,0" Padding="5"/>
            </StackPanel>
            <Frame x:Name="TestCreator" NavigationUIVisibility="Hidden"/>
        </DockPanel>
    </Grid>
</Page>
